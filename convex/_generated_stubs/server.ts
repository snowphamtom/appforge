/** Stubs until `npx convex dev` generates real modules. */
export const query = (def: unknown) => def;
export const mutation = (def: unknown) => def;
export const action = (def: unknown) => def;
export const httpAction = (def: unknown) => def;
